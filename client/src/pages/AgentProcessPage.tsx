/**
 * Agent Process Demo Page
 * 
 * Standalone page for demonstrating live agent execution visualization.
 * Shows the complete OBSERVE → DECIDE → ACT → LEARN cycle in real-time.
 */

import React from 'react';
import AgentProcessVisualizer from '../components/AgentProcessVisualizer';

export default function AgentProcessPage() {
  return <AgentProcessVisualizer />;
}
